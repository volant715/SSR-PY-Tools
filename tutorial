Spanish:
Tutorial:

Instala las dependencias
Python Desde su web o via externa CMD 

Ejecuta el main.py y tendras las herramientas y selecciona el numero que quieras que haga la accion

English:
Tutorial:
Tutorial:

Install the dependencies
Python (from their website or via the command line)

Run `main.py` to access the tools, then select the number corresponding to the action you want to perform.
